<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Switch for flash light -->
    <SwitchPreference
        android:title="@string/pref_title_enable_flashlight"
        android:key="@string/key_flash_light_on"
        android:defaultValue="false"
        />

    <Preference android:title="@string/pref_title_recent_scanned"
                android:key="@string/key_recent_scanned"
        />
</PreferenceScreen>